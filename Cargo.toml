[package]
name = "ram-extractor"
version = "0.1.0"
edition = "2026"

[dependencies]
winapi = { version = "0.3", features = ["winnt", "processthreadsapi", "memoryapi", "handleapi", "psapi"] }
kernel32-sys = "0.2"
